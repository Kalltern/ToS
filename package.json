{
  "name": "tos",
  "version": "3.0.0",
  "description": "CSS compiler for the ToS system",
  "scripts": {
    "build": "sass src/scss/tos.scss css/tos.css --style=expanded --no-source-map",
    "watch": "sass src/scss/tos.scss css/tos.css --style=expanded --source-map --watch"
  },
  "browserslist": [
    "last 3 versions"
  ],
  "author": "Asacolips",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "sass": "^1.53.0"
  }
}